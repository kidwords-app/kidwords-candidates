/**
 * AWS provider stubs.
 *
 * These will implement CandidateRepository (DynamoDB),
 * AssetRepository (S3), and WorkflowClient (SQS) when the
 * production infrastructure is provisioned.
 *
 * See specs/pipeline-04b-backend-architecture.md for the full design.
 */

export {};
